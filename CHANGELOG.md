# 更新记录

## v1.1.1 专属动作与开机自启

- 重做 Weekend 与 INVU 的皮肤专属动作，保持与普通动作一致的人物显示尺度，避免动作播放时人物突然变小。
- 调整两套专属动作的画面差异与连续性，同时保持固定 r15 八张关键姿势和至少 24 个显示阶段。
- 使用说明不再展示面向开发者的皮肤接口段落。
- 右键菜单在公开动态功能下方新增“开机自启”，只作用于当前 Windows 用户并记住开关状态。
- 更新最终单 EXE 与完整 ZIP，并通过双内置皮肤目录、跨皮肤换装、运动网格和运行时自检。

## v1.1.0 双皮肤单 EXE

- 按用户交付要求，将 Weekend 与 INVU 两套完整 176 帧资源及各自运动网格嵌入同一个 `小泰妍.exe`。
- 右键“皮肤”菜单固定显示“Weekend 粉蓝周末（内置）”和“INVU 月影女神（内置）”，不再要求用第二个 EXE 或外置 `skins` 目录切换这两套造型。
- 保留 60 阶段人物轮廓旋身、由头到脚揭示、候选缓存预热和原子缓存替换；切换后的选择继续在重启后恢复。
- QA 窗口改为在两套真实内嵌归档之间切换，不再使用同一皮肤的资源副本。
- 自检新增双资源目录验证、INVU 独立解码与实际 Weekend→INVU 换肤渲染检查。
- 新增交付物 `source/小泰妍-双皮肤单EXE版/小泰妍.exe` 和 `source/小泰妍-双皮肤单EXE版.zip`；ZIP 内只有一个 EXE。

## v1.0.0 小泰妍双造型脚手架

- 产品身份更新为“小泰妍”，互动角色为金泰妍，开发者统一为 `Anbunensi`。
- 内置造型更新为“Weekend 粉蓝周末”。
- 官方外置皮肤清单仅包含 `taeyeon-invu`，显示名“INVU 月影女神”，r15 专属动作名“月影侧身行礼”。
- 主程序输出更新为 `source/outputs/taeyeon-standalone-4k-v1/小泰妍.exe`，皮肤输出更新为 `source/outputs/taeyeon-skins/`，完整包更新为 `小泰妍-完整包.zip`。
- 构建输入更新为 `source/work/taeyeon/weekend-realistic-run/`；运行时源码目录统一为 `source/standalone/xiaotaiyan/`。
- `MIGRATION-001`：将首版历史包清单设为空，历史归档流程本身保持不变，且不会复制小曦薇历史发行。
- 公开动态提醒改为金泰妍相关公开页面与通用文案；未新增网络提供方，启用状态、轮询周期、超时、大小上限、去重、首次基线、退避和可信 URL 校验逻辑保持不变。
- 新增 `identity-replacements.json` 与 `baseline-proof.json`，记录可逆身份替换、固定基线和冻结契约。

## 固定基线继承说明

本产品由提交 `e9b77f9bb106c8e83c3977e853b7f9eba9e6c1bc` 的 v3.0.6 固定基线迁移。以下内容没有重新设计：

- 24 行、176 帧动作契约与各行帧数。
- 状态枚举值、状态转换、点击/拖拽/睡眠/手机/换肤语义。
- 动作时长、至少 24 显示阶段、插值与运动网格算法。
- 随机待机候选、权重和最近动作去重。
- 外置皮肤路径、尺寸、ZIP 条目、总大小和原子加载安全校验。
- 自测、渲染 QA 和网络轮询算法。

本次只完成身份、文案、输入/输出路径和打包清单迁移；没有生成素材、构建或打包。
